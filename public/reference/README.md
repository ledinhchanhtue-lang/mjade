# Reference screenshot

This folder is the intended home for the homepage reference mockup:

```
public/reference/mjade-homepage-reference.png
```

The reference image shared in chat could not be saved to disk automatically —
the assistant only receives attached images as inline vision input, with no
file path or byte access to write them back out.

**To add it:** save the original reference screenshot you shared and place it
at the path above (same filename). It is only used for internal design
comparison and is never imported or rendered by the production site.
